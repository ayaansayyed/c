// Online C compiler to run C program online
#include <stdio.h>

void main() 
{
    // Write C code here
    int a;
    printf("enter any number");
    scanf(" %d",& a);
    if (a>0)
    {
    printf("a is greater than 0 that mean a positive number ");
    }
    else if(a<0)
    {
    printf("a is lesser than 0 that mean a negative number ");
    }
    else
    {
      printf("a=0");  
    }
}
